import { HomePage } from '../page';

export default function HomeRoute() {
  // This route ensures the navigation component is displayed
  // as it's part of the (main) layout
  return <HomePage />;
}